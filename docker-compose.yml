services:
  json-repair:
    image: json-repair
    container_name: json-repair
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8080"
    environment:
      - NODE_ENV=production