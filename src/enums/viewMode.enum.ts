export enum ViewMode {
  Editor = "editor", // Editor only - no split view
  Graph = "graph",
  Tree = "tree",
  Compare = "compare",
  Viewer = "viewer", // Keyboard-navigable JSON viewer
  Table = "table", // Spreadsheet-like view for JSON arrays
}
